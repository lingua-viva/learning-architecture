# UI Bundle Contract — Lingua Viva (MC-lessons §3, 2026-07-19)
#
# Protects the teacher-facing UI (static/index.html — the whole 49KB+
# sidebar app, static/sw.js — the PWA service worker) and its server
# (src/web.py, 40+ endpoints) from silent drift. Cloned from Mission
# Canvas's own proven WIZARD_CONTRACT.yaml / check_wizard_contract.py shape
# (dev practice: clone, don't redesign installers/contracts).
#
# Any change to a listed file requires a deliberate version bump:
#   python3 scripts/check_ui_contract.py --bump
#
# CEREMONY RULE: any build step that writes static/ (npm run build under
# desktop/, hand edits to index.html/sw.js, endpoint additions to web.py)
# must be followed by a contract re-lock. Lock AFTER the build, never
# before — MC's v27->v28 trap was a desktop build regenerating static asset
# hashes after the lock was already taken, which preflight then caught.
#
# Bump log (one line per deliberate change — the comments are the changelog):
#   v1 (2026-07-19): initial lock, MC-lessons pass baseline.
#   v2 (2026-07-19): src/web.py — added _log_request_outcome middleware
#     (MC-lessons §5, request-outcome NDJSON log + zero-500 health check).
#   v3 (2026-07-19): static/index.html — added Ctrl/Cmd+K quick-capture
#     overlay, posting free text through /api/query (MC-lessons §7).
#   v4 (2026-07-19): src/web.py — added GET /api/profile/export; static/index.html
#     — added Export My Data button before Clear All Data (MC-lessons §8).
#   v5 (2026-07-20): src/web.py — moved default ingest scratch storage under
#     Lingua Viva runtime home instead of the legacy case-study data tree.
#   v6 (2026-07-20): src/web.py/static/index.html — admin deferred views now
#     return and render explicit reasons/prerequisites instead of placeholders.
#   v7 (2026-07-20): static/index.html — LV P0 improvement cycle EXP09 fix:
#     added healthBadgeClass() helper + .badge.risk CSS so Health view's
#     PRIVATE_RISK status no longer shares a CSS class with routine WARN
#     (SPEC_LV_P0_IMPROVEMENT_CYCLE_2026-07-20.md).
#   v8 (2026-07-20): static/index.html/src/web.py — Claudia-lens hardening:
#     teacher-facing copy/register updates, warmer parent-draft route copy,
#     and quick-capture deterministic trace feedback.
#   v9 (2026-07-20): static/index.html — sidebar accessibility + token pass:
#     nav aria labels/current state, visible focus styling, and documented
#     nav tuple/state contract.
#   v10 (2026-07-22): src/web.py/static/index.html - file-map verification:
#     explicit domain confirmation, opt-in student-zone metadata peek, and
#     manual file-to-student assignment.
#   v11 (2026-07-22): src/web.py/static/index.html - Slack integration:
#     signed Events API receiver, secret-free status route, and Slack app view.
#   v12 (2026-07-22): Slack 15-pass hardening:
#     precise channel scopes and path-only reverse-proxy safety guidance.
#   v13 (2026-07-22): convergence re-lock after concurrent file-map UI
#     accessibility/error-handling and student-assignment work landed.
#   v14-v18 (2026-07-22): concurrent convergence plus Observe field suggestions,
#     local-only classification, live lens context, resilient Oka-style voice in Ask,
#     and the final combined protected-file re-lock.
#   v19 (2026-07-22): 15-pass Observe/Ask Oka voice hardening:
#     bounded classification, truthful degrade states, resilient speech lifecycle,
#     explicit review gates, accessibility, and visible save/network recovery.
#   v20 (2026-07-23): teacher-lens/RTI API endpoints (PUT rti, GET lens-as-of)
#     + Phase 5B surface cards (Differentiated Groups, My Teaching Style).
#   v21 (2026-07-23): add Linux download button to landing page.
#   v22 (2026-07-23): Observe support-profile classification write path:
#     teacher-reviewed support entries, guidance, feedback, and lens display.
#   v23 (2026-07-23): Google Drive explicit-import connector mounted in
#     Settings with status, list, select, purpose, student assignment, and
#     import-result UI call sites.
#   v26 (2026-07-23): Restored unobserved_students return statement in web.py.
#   v27 (2026-07-23): Fixed support-profile summary JavaScript parse error
#     found during Google Drive 15-pass hardening.
#   v28 (2026-07-23): Ingestion and extraction mapping v2 UI: Extraction Sources, Run Extraction, Review Extraction modal, and Write Confirmed to Lens.
#   v29 (2026-07-23): LV-BLT-001 provider connect form + LV-BLT-003 teaching artifact ingest UI in Settings.
#   v30 (2026-07-23): mount /api/stats in Health as a fail-open System panel
#     with six system-scale cards (LV-BLT-007); mount direct RTI tier updates
#     in the Students lens with constrained choices, refresh, and visible
#     error states (LV-BLT-006); removed the genuinely duplicate,
#     never-mounted GET /api/students/unobserved route — confirmed dead via
#     ROUTE_REACHABILITY.yaml and brief.py's independent _unobserved()
#     implementation, which is what Home's observation reminder actually
#     calls (LV-BLT-005 reclassify); removed duplicate GET /api/teacher/today
#     and its orphaned private schedule-resolution helpers (Home retains
#     /api/brief); docstring added to session_info().
#   v31–33 (2026-07-23): re-seal lock after pull --rebase merged remote's v30
#     (stats panel, RTI tier, route removals) with local's v29 (BLT-001/003
#     provider connect + teaching ingest); LV-BLT-009 Knowledge browser
#     (admin view with /api/ontology/domains endpoint + domain/node grid);
#     BLT-006 final: reclassified curriculum/unit and lens-as-of as permanent
#     backend-only (7 deferred_undecided remain). v33: fix OntologyNode field
#     name (name, not label) — caught by live endpoint simulation.
#   v34 (2026-07-27): Drive round-trip for teacher testing: extraction
#     sources now dual-scan imports/ + runtime/drive_imports/ (path-mismatch
#     fix — Drive imports never surfaced for extraction); new explicit
#     POST /api/google-drive/upload (student lens JSON or lv-home deliverable
#     into the configured folder only, privacy-logged) + Share-back-to-Drive
#     panel in Settings.
#   v35 (2026-07-27): One-button update (SPEC_ONE_BUTTON_UPDATE_2026-07-27):
#     src/web.py — P0 reflection-log relocation to lv_home() + legacy
#     migration, startup reconcile hook, GET /api/updates/pending,
#     GET /api/updates/diff, POST /api/updates/resolve; static/index.html —
#     Settings "Template Updates" panel (Keep mine / Take new / View diff).
#     Lock also seals the concurrent Drive-workspace lane's in-flight
#     index.html edits (same working tree, single lock).
#   v36 (2026-07-27): Drive workspace (SPEC_LV_DRIVE_WORKSPACE_2026-07-27
#     Phase 1): Drive is now a first-class utility-nav view (was a Settings
#     panel) — connect folders by pasted Drive URL (live-verified, purpose-
#     first), teacher-language file labels (Google Doc / Ready to read),
#     imports end with a "Review now" next action that pre-selects the file
#     in extraction, share-back panel with connected-folder destinations;
#     src/web.py — GET/POST /api/google-drive/folders +
#     DELETE /api/google-drive/folders/{folder_id}, list route stamps
#     last_checked on connected folders.
#   v37 (2026-07-27): Slack Daily Operations Assistant Lane E
#     (SPEC_LV_SLACK_OPS_ASSISTANT_2026-07-27): src/web.py — Socket Mode
#     startup/shutdown wiring (SlackSocketClient + SlackOpsBot + briefing
#     scheduler, cleanly off when unconfigured), GET /api/slack/ops/status
#     (secret-free), GET /api/ops/daily (per-teacher daily-file render),
#     GET /api/ops/records (local audit); static/index.html — new "Daily"
#     teacher-nav view rendering the same markdown the assistant writes to
#     the Desktop, with connection checklist + privacy boundary.
#   v38 (2026-07-27): re-seal after both concurrent lanes finished — the v37
#     lock was taken while the Slack-ops lane's index.html/web.py edits and
#     the Drive-workspace lane's final extraction-preselect edit were still
#     landing in the shared working tree. No new features in this bump;
#     lock now matches the settled tree (Drive workspace v36 + Slack ops
#     Daily view v37 together).
#   v39 (2026-07-27): Sources view + File Map UX redesign
#     (SPEC_LV_SOURCES_VIEW_FILE_MAP_UX_2026-07-27): static/index.html —
#     new top-level "Sources" view (Slack status card + relocated Drive
#     panel + redesigned Local folders), "drive" nav id retired, Settings
#     drops Curriculum-folders + Drive panels; file-map invitation state
#     (Choose a folder / Use Documents-Teaching / Skip for now), pre-scan
#     reassurance copy, depth moved to Advanced disclosure, post-scan
#     headline + Teaching materials / Private student folders split,
#     "Show names only" peek with humanized metadata, link-file language,
#     inline reset confirm, friendly filemap errors, Home nudge card;
#     src/web.py — stable `code` field on filemap error responses
#     (not_found / permission_denied / invalid_path). NOTE: this bump
#     landed over the v38 concurrent-lane seal — v38's lock was taken
#     while this lane's edits were still in flight.
#   v40 (2026-07-27): Drive-surface hardening loop re-seal
#     (dev/HARDENING_LOOP_DRIVE_2026-07-27.md, 15 iterations, 8 fixed /
#     7 verified-OK). src/web.py only: ValueError→400 on Drive list,
#     privacy events on folder connect/disconnect + file import, import
#     payload validation (empty/non-string file_ids → 400 instead of 500),
#     LV_LOCAL_IMPORTS_DIR hermeticity seam for the extraction-sources
#     demo-file fallback. No UI changes; index.html/sw.js unchanged.
#   v41 (2026-07-27): One-button-update hardening loop iteration 7
#     (REPORT_ONE_BUTTON_UPDATE_2026-07-27.md §8): src/web.py —
#     _migrate_legacy_revision_log now dedupes revision_ids WITHIN the
#     legacy file (same-file duplicate migrated twice pre-fix) and
#     catches UnicodeDecodeError alongside OSError on a non-UTF8 legacy
#     log (retry-next-launch, sentinel unwritten). No UI changes.
#     NOTE: v40's lock (Drive lane) was taken after these edits landed
#     in the shared tree and silently sealed them; this bump makes the
#     change deliberate per the ceremony rule.
#   v42 (2026-07-27): Slack Ops Assistant 15-pass hardening loop
#     (dev/reports/REPORT_SLACK_OPS_HARDENING_2026-07-27.md): src/web.py —
#     _shutdown_slack_ops now awaits the cancelled briefing-scheduler task
#     (no destroyed-pending-task warnings at shutdown). No UI changes.
#     NOTE: v41's lock (one-button-update lane) was taken after this edit
#     landed in the shared tree and silently sealed it; this bump makes
#     the change deliberate per the ceremony rule.
#   v43 (2026-07-27): One-button-update review finding 2 fix: src/web.py —
#     reflection revision_id now uuid4 (int(time.time()) collided for two
#     saves in the same second) and the P0 migration dedupes on
#     (revision_id, content-hash) so distinct same-second reflections
#     both survive while byte-identical double-submits still collapse.
#     No UI changes.
#   v44 (2026-07-27): In-app Google sign-in for Drive
#     (SPEC_LV_DRIVE_SELF_SERVICE_AUTH_2026-07-27 §A). src/web.py: POST
#     /api/google-drive/auth/start (PKCE loopback flow, backend-side
#     browser open) + POST /api/google-drive/auth/disconnect (revoke +
#     delete stored token, drive_account_disconnected privacy event).
#     static/index.html: Sources/Drive auth panel — Sign in with Google
#     (+ verbatim restricted-scope trust copy), 2s status polling,
#     "Sign in again" on needs_signin, Signed-in-as/Sign-out, env
#     credentials shadow and hide the sign-in controls.
#   v45 (2026-07-27): Live-layer read path (SPEC_LIVE_LAYER_READ_PATH
#     _2026-07-27 §3): static/index.html — Template Updates panel take-new
#     success message now carries the restart hint ("Saved — restart Lingua
#     Viva to use the new version.") since live-layer files now actually
#     feed lens behavior; keep-mine message unchanged (kept copy is already
#     the one serving — a restart hint there would be false). No web.py
#     changes (reader-only build, zero new routes).
#   v46 (2026-07-27): Drive final hardening H1-H5
#     (SPEC_LV_DRIVE_FINAL_HARDENING_2026-07-27). static/index.html: H1
#     unverified-app interstitial walkthrough copy in the sign-in panel
#     ("Advanced" -> "Go to Lingua Viva (unsafe)"; OAuth client must be
#     named "Lingua Viva" in the console for the copy to match).
#     src/web.py: H3 — materialize now writes student lens snapshots via
#     _atomic_write_private_json (0600-from-birth, was default umask) and
#     the upload route prunes drive_exports to the 3 newest per student
#     after a successful upload (imports are never pruned). Zero new
#     routes. Backend-only (no contract files): H2 100MB import cap with
#     code "file_too_large", H4 query length bound, H5 malformed-response
#     guards + loopback listener cleanup pin.
#   v47 (2026-07-27): Slack Ops v2 Workflow Packs Phase 2+3
#     (SPEC_LV_SLACK_OPS_V2_WORKFLOW_PACKS_2026-07-27 §3.2, §6).
#     src/web.py: startup installs the bot-spec compile (v1 parity when no
#     file, fail-closed on malformed), go-live gate (bot-spec exists +
#     live:false keeps the transport off; env-only setups unchanged),
#     run_schedules now takes briefing/EOD times from the bot-spec (closes
#     v1 known-limit #2); new Bot Setup routes GET /api/ops/setup/catalog,
#     GET+PUT /api/ops/setup/bot-spec (PUT validates-by-compiling before
#     writing, preserves teach-loop fields, 409s go-live without a passing
#     corpus run, atomic in-process swap, never touches the transport),
#     GET /api/ops/setup/roster (read-only, env stays source of truth).
#     static/index.html: Bot Setup panel as a Daily sub-view (packs
#     checklist, briefing/EOD times, review-required categories, read-only
#     roster, go-live gate status). Teacher-visible Daily behavior
#     unchanged by design — new "Bot Setup" button only.
#   v48 (2026-07-27): Slack Ops v2 Workflow Packs Phase 4+5
#     (SPEC_LV_SLACK_OPS_V2_WORKFLOW_PACKS_2026-07-27 §4, §7).
#     src/web.py: teach loop POST /api/ops/review/reclassify (record fix
#     + optional candidate rule; coverage-machine records guarded); corpus
#     gate POST /api/ops/setup/corpus/run (records last_run — the go-live
#     evidence), POST /api/ops/setup/corpus/sentences (admin sentence,
#     clears the stale run), POST /api/ops/setup/rules/decide (corpus-gated
#     candidate approve/reject — approve runs the corpus against a
#     hypothetical compile and refuses atomically on any failure); PUT
#     bot-spec staleness guard drops last_run when pack membership or
#     period aliases change. static/index.html Bot Setup panel: To Review
#     teach panel, Run-corpus-test table, add-test-sentence form,
#     Go live / Pause button, Approve/Reject on candidate rules.
#     Teacher-visible Daily behavior still unchanged — admin panel only.
#   v49 (2026-07-27): Slack Ops v2 packs Phase 6 stretch
#     (SPEC_LV_SLACK_OPS_V2_WORKFLOW_PACKS_2026-07-27 §5 packs 6-7, §8).
#     src/web.py: GET /api/ops/setup/suggestions — shadow suggester,
#     weekly would-have-matched counts of unmatched traffic (other +
#     positional announcement) against disabled packs' vocabularies;
#     counts and pack names only, never record text. static/index.html:
#     Bot Setup panel renders the suggestion line above the packs
#     checklist (suggestion only — enabling stays checklist + Save +
#     corpus run). Backlog packs bus_transport/dismissal_changes ship as
#     data (enabled_by_default: false — v1-parity compile untouched).
#   v50 (2026-07-27): LV-5 startup blocker fix (desktop-v0.2.12).
#     src/web.py: the three app.add_event_handler calls now target
#     app.router — Starlette 1.0 removed the method from the application
#     object, which crashed the backend at import on every fresh install
#     (unpinned installer pulled starlette 1.3.1 via latest fastapi). No
#     route, UI, or behavior change; verified booting on starlette 0.50.0
#     AND 1.3.1. Paired with pinned installer deps in bootstrap.ts + a
#     backend-boot smoke gate in desktop-release.yml.
#   v51 (2026-07-28): Italian STT fix (SPEC_WORKSTATION_BUILD_ORDER_2026-07-28
#     "fix immediately" item). static/index.html: recognition.lang in both
#     voice handlers — toggleAsk (Talk to Lingua Viva mic) and toggleObserve
#     (Observe mic) — changed "en-US" -> "it-IT". Teachers speak Italian; the
#     Web Speech recognizer was transcribing their Italian against an English
#     acoustic/language model, so observation dictation and Ask both returned
#     garbled text. No route, layout, or handler-flow change — one string per
#     handler. NOTE: TTS playback (voiceRuntime speak) still selects an
#     English voice; tracked separately, not part of this bump.
#   v52 (2026-07-28): Italian TTS — the other half of the v51 voice fix.
#     static/index.html voiceRuntime.speak(): playback selected an English
#     voice by name (samantha|aria|serena|google uk english female) and fell
#     back to any /^en[-_]/ voice, so after v51 a teacher spoke Italian and
#     was answered in English. Now sets utterance.lang = "it-IT" BEFORE
#     reading the voice list (getVoices() returns [] until Chrome loads
#     voices asynchronously; lang alone then still drives an Italian default
#     rather than an English one), then prefers a named Italian voice
#     (alice|federica|google italiano), else any it-* voice, else null.
#     No route, layout, or handler-flow change.
#   v53 (2026-07-28): Slice 5 Credentials — in-app Slack setup
#     (SPEC_CONNECTION_CREDENTIAL_SETUP_2026-07-28 §Lingua Viva Application).
#     The Daily Operations Assistant could previously only be configured by
#     exporting four LV_SLACK_* environment variables in a terminal, which
#     made it unreachable for the teachers starting Monday. src/web.py:
#     GET /api/slack/credentials (secret-free per-field status + source),
#     PUT (validate, store 0600, reconnect immediately), DELETE (forget +
#     disconnect), POST /api/slack/credentials/test (Slack auth.test, saves
#     nothing). All four are loopback-guarded and return {"error": ...}
#     bodies, the shape static/index.html's api() helper actually reads.
#     src/lingua_viva/slack_credentials.py (new) resolves env > stored >
#     unset per variable, matching the Drive precedent; slack_socket.py
#     gains _resolved_env() so require_ops_config()/ops_status() read the
#     store, plus a module-level auth_test(). static/index.html: Settings ->
#     Integrations panel — Slack form (env-managed fields render disabled and
#     labelled, saved secrets show as empty "leave blank to keep" boxes
#     because the page never receives them) and a Google Drive status card
#     pointing at the existing Sources -> Drive sign-in.
#   v54 (2026-07-28): Slice 4 Governance control plane
#     (SPEC_GOVERNANCE_CONTROL_PLANE_2026-07-28 §Lingua Viva Application).
#     src/lingua_viva/governance.py (new): Trust Status answering the five
#     questions a school administrator asks, counted from real local
#     artefacts — and reporting "unknown" when the student database cannot
#     be read rather than printing a comfortable 0. Sealed observation packs
#     (HMAC-SHA256 on a machine-local 0600 key, verify_pack() really
#     verifies) carrying an anonymous student reference, with confirmed-by-
#     teacher separated from model-suggested-not-confirmed. Publication
#     safety enforces the two mechanically decidable rules from
#     governance/publication_safety.yaml (student names, AI attribution) and
#     reports the rest as advisory instead of implying it cleared them.
#     src/web.py: GET /api/governance/trust, POST
#     /api/governance/observation-export (refuses on violation rather than
#     silently dropping the field), POST /api/governance/verify-pack.
#     static/index.html: Governance utility-nav view merging Trust Status +
#     export + evidence tabs over privacy log / traces / Doctor.
#     Also removed three claims the old views rendered regardless of fact:
#     a hardcoded "external calls: 0" badge in Why, the external-call count
#     labelled "No external calls" in Privacy, and Privacy's flat "No data
#     has left this machine." — which was untrue until 53360dc.
#   v55 (2026-07-28): Slice 1 Sources — unified source registry + measured
#     provenance (SPEC_UNIFIED_SOURCES_WORKBENCH_2026-07-28 §Lingua Viva).
#     src/web.py: GET /api/sources/status aggregating local folders, Drive
#     and Slack into one row-per-source registry with status, count and
#     excluded student zones; each source resolves independently so one
#     outage degrades its own row (count null, "could not read") instead of
#     reporting a false 0. Provenance is now measured rather than asserted:
#     the query response's route/external_called/external_calls were
#     hardcoded local/False/0, and src/lingua_viva/traces.py destroyed the
#     same receipt twice more — new_trace() pinned them at write time and
#     read_traces() overwrote them again at read time, so the Why view could
#     only ever say "local". All three now derive from the model that
#     actually answered. static/index.html: registry panel at the top of
#     Sources with the student-zone privacy badge, and the Ask route badge
#     no longer defaults to "local" when the field is absent (it reads
#     "route not recorded").
#   v56 (2026-07-28): Slice 3 Action Queue — "Activity" utility-nav view
#     (SPEC_NATIVE_WORKSTATION_SURFACES_2026-07-28 §Lingua Viva item 1).
#     src/lingua_viva/activity.py (new) + GET /api/actions/history: a feed of
#     what happened on this computer and what is still waiting, merged from
#     the existing trace and privacy logs so it has real history on day one
#     rather than being empty until a new store fills. Honours that spec's LV
#     constraint — these surfaces get projected in classrooms, so no question
#     text and no student names appear; pending items use the same anonymous
#     reference as the governance packs. An unreadable student record yields
#     an explicit "unknown" entry, because an empty pending list would read
#     as "you are caught up", which is a claim.
#   v57 (2026-07-28): Slice 2 Dispatcher — teacher action registry +
#     governance preview (SPEC_ACTION_DISPATCHER_FORMS_2026-07-28 §Lingua
#     Viva items 1 and 3; item 2's parameter forms already existed in
#     Prepare and Assess). src/lingua_viva/actions.py (new): 8 code-defined
#     actions, each naming the route that implements it and declaring what it
#     reads and whether anything leaves the machine. Exactly one —
#     Drive upload — is marked outbound, and says so plainly in its preview.
#     preview_for() returns None for an unknown id rather than a generic
#     reassurance, per the build order's no-fake-affordance law. src/web.py:
#     GET /api/actions/registry. static/index.html: "What you can do" panel
#     in Activity, and an inline preview in Prepare before Generate.
#   v58 (2026-07-28): Slice 6 Daily augmentation
#     (SPEC_NATIVE_WORKSTATION_SURFACES_2026-07-28 §Lingua Viva item 2).
#     src/web.py: GET /api/daily/briefing — three morning-briefing widgets
#     (not observed in N days, support decisions waiting, suggestions
#     awaiting confirmation) built on BriefService._unobserved and
#     ._rti_pending, which already existed. Students are referenced
#     anonymously: Daily is read in staff rooms and projected in meetings,
#     so the same constraint applies as to the Activity view. An unreadable
#     student record returns readable=false with an explicit note, because
#     an empty widget set would read as "nothing waiting".
#     static/index.html: "Your day" panel above the Slack schedule.
#   v59 (2026-07-29): Gap 1 parent report safety gate
#     (SPEC_LV_REMAINING_GAPS_2026-07-29 Gap 1, P0). src/web.py:
#     POST /api/parents/recommendation now runs check_publication_safety()
#     on the POST-strip draft — the words that actually reach the teacher.
#     _strip_parent_output() does case-sensitive exact replacement, so
#     "Marco" was removed while "marco" reached the screen unflagged; the
#     gate lowercases before matching and catches that class of miss. Both
#     run: the stripper fixes what it can, the gate reports what it could
#     not. Flags (safety_warnings + review_required), never blocks — a draft
#     the teacher cannot see is one they cannot fix, and nothing reaches a
#     parent until they send it. static/index.html: warnings render in a
#     red-bordered block ABOVE the draft, not as a dismissible toast.
#     governance.py: the shared violation message is now context-neutral,
#     since this gate serves both observation exports and parent drafts and
#     they need different remedies.
#   v60 (2026-07-29): Gap 2 filemap auto-scan
#     (SPEC_LV_REMAINING_GAPS_2026-07-29 Gap 2). src/web.py: new
#     _startup_filemap_autoscan handler starting a daemon thread so the file
#     map populates on first launch without the teacher pressing anything.
#     Never awaited — a home-folder scan takes seconds to minutes and the app
#     must be usable immediately. src/lingua_viva/filemap.py:
#     auto_scan_on_startup(), is_scan_in_progress(), roots_needing_rescan(),
#     _scan_root_is_stale(). Guarded by LV_AGENT=1 (new, documented
#     convention mirroring MC_AGENT) AND by a pytest check, so a test that
#     forgets the env var still cannot scan a developer's real home folder.
#     No route added. The spec's instruction to port MC's
#     auto_scan_on_startup() could not be followed — MC has no such symbol —
#     so this is written for LV's shape.
#   v61 (2026-07-29): Gap 3 ARON naming
#     (SPEC_LV_REMAINING_GAPS_2026-07-29 Gap 3). governance.py:
#     anonymous_student_ref() renamed aron_ref() with an ARON docstring, the
#     old name kept as an alias, plus ARON_EXPLANATION and aron_status().
#     src/web.py: GET /api/privacy now includes an "aron" block (no new
#     route). static/index.html: Privacy view states ARON is active and what
#     it means; every place an S-XXXX code appears — Activity pending items,
#     Daily briefing badges, observation-export pack heading — carries a
#     tooltip explaining it and that it cannot be reversed into a name.
#   v62 (2026-07-29): Gap 4 admin views with real data
#     (SPEC_LV_REMAINING_GAPS_2026-07-29 Gap 4, Option A). Evidence,
#     Capacity and Trends were honest "deferred" stubs — three nav items that
#     did nothing. src/lingua_viva/admin_metrics.py (new): observation
#     coverage per student (ARON) and per domain; recorded activity per week
#     aggregated from the same activity_feed the Activity view renders, so
#     the two cannot disagree; support-tier distribution withheld below a
#     minimum cohort. src/web.py: the three routes now return those metrics
#     and the dead _admin_deferred() helper is gone. static/index.html:
#     renderAdmin dispatches to three simple table renderers — no charts.
#     The honesty requirement moved rather than vanished: empty states state
#     a reason, unreadable records report unknown rather than zero, Capacity
#     says in the payload that it is not a staffing model, and Trends notes
#     that Tier 1 is a default rather than a finding.
#   v63 (2026-07-29): Gap 5 Rime TTS with student-data privacy gate
#     (SPEC_LV_REMAINING_GAPS_2026-07-29 Gap 5). src/web.py: POST
#     /api/voice/tts. Rime is an external service, so this is an egress path:
#     check_publication_safety() runs BEFORE the API key is even read, and a
#     student name means the request never leaves this machine (403,
#     fallback=local, privacy event voice_kept_local_student_data). An
#     unreadable roster also fails closed — without it we cannot prove the
#     text names no child. Successful sends log
#     voice_sent_to_external_tts so the governance view can count them.
#     static/index.html: voiceRuntime.speak() now tries the endpoint and
#     falls back to speakLocally(), which keeps the v52 Italian voice — the
#     fallback is the private path, not a degraded one. stopSpeaking() also
#     halts Rime playback so the two voices cannot talk over each other.
version: 63
files:
  - static/index.html
  - static/sw.js
  - src/web.py
